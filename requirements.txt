# AI Short-Form Video Editor — Python dependencies
# Installed automatically by setup.sh / the Codespaces devcontainer.
#
# NOTE (Codespaces disk): torch is large (~2 GB). On tiny machines you can
# swap it for the CPU-only wheel before running setup.sh:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu

# Web UI
streamlit>=1.30

# Local CPU transcription (OpenAI Whisper)
openai-whisper>=20231117

# Whisper's neural-network runtime (CPU wheels are selected automatically)
torch
torchaudio

# Video / audio processing helpers
moviepy>=2.0.0
ffmpeg-python

# Optional heavy-duty silence cutting. The app uses a built-in FFmpeg
# `silencedetect` pipeline by default (see src/silence_remover.py);
# auto-editor is installed so its heuristics can be layered in later.
auto-editor

# Text overlay rendering (hook banner + word highlighting)
Pillow>=10.0.0
numpy>=1.24,<3
